using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyDestination : MonoBehaviour
{
    [SerializeField] private SetPosition setPosition;
    private Vector3 destination;                            //目的地
    private Vector3 playerDestination;                      //プレイヤーの位置

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
